@model IEnumerable<Muse.Models.TvShow>

@{
    ViewBag.Title = "AddTvShow";
}

<h2>AddTvShow</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstAired)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Network)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstAired)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Network)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TVDB_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TVDB_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TVDB_ID })
        </td>
    </tr>
}

</table>
